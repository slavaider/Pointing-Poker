import React, { FC } from 'react';
import styles from './Footer.module.scss';

// todo переписать текст!!!
const Footer: FC = () => <footer className={styles.footer}>Team A</footer>;

export default Footer;
